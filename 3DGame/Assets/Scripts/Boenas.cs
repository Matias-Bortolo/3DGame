using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Boenas : MonoBehaviour
{
    //Variables privadas empezar con _ (guion bajo)
    [SerializeField] //Visualizar variables en el editor de Unity para modificar facil
    private float _life;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
